h4 Child
code
  | {{ localItem | json}}
div
  label Child Three:
  input(type='text', placeholder='child', name='text-item-two', [(ngModel)]='item.three')
  label Child Four:
  input(type='text', placeholder='child', name='text-item-two', (keyup)='onKey($event)', [(ngModel)]='localItem.four')
  em * This is updates parent item three via event
